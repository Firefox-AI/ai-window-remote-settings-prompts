Page title:
{title}

Page url:
{url}

Fields to classify:
{fields}

Valid field types (choose only from these):
{fieldTypes}

Guidance:
- Classify each field using its own attributes plus the form content and page
  context above.
- Pick the closest matching type; if nothing fits, use the unknown/other type
  when one is available.
- Include every field id exactly once and only use the ids listed above.
- Set "confidence" to "high" (an unambiguous signal maps directly to the type),
  "medium" (inferred from context, a plausible alternative exists), or "low" (a
  weak guess, or you fell back to the unknown/other or closest type).
