Below is a list of statements and their reasoning. Find sets of statements that describe the same specific pursuit — the same object, activity, project, or goal — not merely the same broad subject area.

Do NOT group statements because they share a category. "Health", "shopping", "technology", "travel", "finance", and "research" are categories, not themes.

Use the new statement as the test, and write it before you commit to a group: if you cannot say what the statements have in common without a category word ("health", "technology", "information", "topics", "protocols", "advancements") or without an "and" joining two different pursuits, the statements do not belong together. Discard the group.

Some statements will not belong to any group. Returning few groups, or none at all, is a correct answer. A group that merges unrelated statements is a worse error than a group you failed to find, so when in doubt, leave the statements ungrouped.

When you identify a grouping, write each of the grouped statements, a new reasoning for the grouping, and a new, high level statement that represents the new group.
New statements must be 4-10 words. Match the level of specificity in these examples — each names concrete brands, products, or activities rather than a category (for reference only; do NOT copy):
- "Prefers LLBean & Nordstrom formalwear collections"
- "Compares white jeans under $80 at Target"
- "Streams new-release movies via Fandango"
- "Cooks Mediterranean seafood from TasteAtlas recipes"
- "Tracks minimalist fashion drops at Uniqlo"

Write your new reasoning as a summary of the component statement reasonings. Do not refer to the statements, themselves.

Every group must contain at least two statements. If a statement is unique and cannot be grouped, do not create a group for it alone.

Return ONLY JSON per the schema below:
```json
[
    {
        "component_statements": [
            ...
        ],
        "new_reasoning": ...,
        "new_statement": ...
    },
    ...
]
```